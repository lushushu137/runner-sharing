# 共享 Skills（上下文与协作）

这些 Skill 是所有职能 Agent 通用的“操作协议”，用于统一输入输出，避免各写各的。

## 列表

- `context-load.md`：会话启动上下文加载
- `story-ingest-to-package.md`：故事到内容包生成流程
- `task-report.md`：统一任务汇报格式
- `handoff-generate.md`：跨职能交接格式
- `validate-preflight.md`：提交前一致性检查

## 使用原则

1. 先加载上下文，再开始执行
2. 每次输出都引用文件路径
3. 先校验后交付
